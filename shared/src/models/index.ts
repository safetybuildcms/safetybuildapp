// must import type samples to ensure that the type is registered
// type annotations are ignored at runtime so don't cause an import
import { Document, DocumentTypeSample, DocumentStatus, DocumentType } from './document'
import { Contractor, ContractorTypeSample } from './contractor'
import { Company, CompanyTypeSample } from './company'

export {
  Document,
  DocumentTypeSample,
  DocumentStatus,
  DocumentType,
  Contractor,
  ContractorTypeSample,
  Company,
  CompanyTypeSample
}
